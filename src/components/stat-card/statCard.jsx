import React from "react";
import "./statCard.scss";
import { Typography, Card, CardContent } from "@material-ui/core";

const statCard = ({ title, cases }) => (
  <Card className="card__container">card</Card>
);

export default statCard;
